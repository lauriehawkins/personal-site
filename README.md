# Personal Site

A simple multi-page personal website with sections for:
- Home/Intro
- Fitness
- Nerd Stuff

## Running Locally

Simply open `index.html` in a browser, or use a local server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`
